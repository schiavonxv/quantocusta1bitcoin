<!-- Header -->
<%= render 'header' %>

<main>
    <!-- Cotação do site -->
    <%= render 'cotacao' %>

    <!-- Como comprar Bitcoin? -->
    <% if false %>
        <!-- Remover if false para rendezir essa section -->
        <%= render 'comprar' %>
    <% end %>

    <!-- Sobre -->
    <%= render 'sobre' %>
</main>
